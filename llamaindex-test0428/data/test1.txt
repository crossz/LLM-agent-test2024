我现在需要帮用户创建一个基于LlamaIndex v0.12的演示程序。首先，我得回顾一下用户之前的问题和搜索结果，看看有哪些可以参考的信息。用户之前问过如何将旧代码适配到v0.12，现在需要的是一个全新的demo。

首先，我应该检查LlamaIndex v0.12的官方文档，但根据用户提供的搜索结果，比如1、2和7中的内容，这些资源提到了使用SimpleDirectoryReader加载数据、创建索引以及查询引擎的基本步骤。例如，7中的代码示例使用了VectorStoreIndex和查询引擎，这可能是新版本的标准做法。

接下来，我需要确认v0.12中的模块结构。在之前的回答中，用户提到类名和导入路径的变化，比如GPTListIndex改为ListIndex，但现在可能需要用VectorStoreIndex，因为7中的示例使用了它。此外，设置全局ServiceContext的方法可能仍然适用，如使用set_global_service_context。

然后，考虑数据加载部分。SimpleDirectoryReader仍然适用，如1、2和7中所示。需要确保数据目录正确，并处理不同文件格式的支持。

关于模型配置，用户之前使用了OpenAILike和不同的API端点，比如百度或SiliconFlow。在demo中，可能需要使用OpenAI的默认设置，但用户可能希望展示如何自定义，比如设置api_base和api_key，如2中的示例。不过，为了简化demo，可能使用默认的OpenAI模型，并提示用户替换API密钥。

然后，构建索引部分。v0.12中创建索引的方式可能有所不同，比如使用VectorStoreIndex.from_documents ，并传入ServiceContext。需要确保正确导入ServiceContext和LLM相关模块，如OpenAI或自定义的类。

查询引擎的创建和查询步骤在7中有示例，使用index.as_query_engine() 和query_engine.query() 方法。需要确保这部分代码正确，并处理响应输出。

另外，保存和加载索引的功能可能对用户有用，如3中提到的保存到磁盘。可以在demo中加入这部分，展示如何持久化索引。

还需要注意错误处理，比如网络连接问题或API密钥错误，如1和6中提到的。在demo中加入try-except块或提示用户检查这些点可能有助于用户体验。

最后，综合多个搜索结果的信息，比如4和8中关于LlamaIndex的基本概念和安装步骤，确保demo包含必要的安装说明和环境设置。

现在，我需要将这些步骤整合成一个简洁的示例代码，确保所有导入正确，使用v0.12的API，并包含必要的注释和错误处理。同时，参考用户历史对话中的代码结构，确保风格一致。可能还需要测试代码片段，确保没有语法错误或过时的方法。